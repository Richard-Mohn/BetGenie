import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BetGenie — AI-Powered Sports Betting Intelligence",
  description:
    "The first platform that analyzes the whole player — personal life, psychology, emotions — to find betting edges the market misses. By Mohn Empire.",
  keywords: [
    "sports betting",
    "AI",
    "player props",
    "parlays",
    "player impact score",
    "betting analytics",
    "Mohn Empire",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
